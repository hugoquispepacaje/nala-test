import colors from '../../style/colors';

const styles = {
  appContainer: {
    width: '100%',
    minHeight: '100vh',
    height: '100%',
    backgroundColor: colors.background,
  },
};

export default styles;
